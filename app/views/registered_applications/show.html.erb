<div class="container"
  <div class="media">
    <div class="media-body">
      <h1 class="media-heading">
          <%= @app.name %>
          <br>
          <small>
            <%= @app.url %><br><br>
          </small>
          <%= link_to "Delete Application", user_registered_applications_path, method: :delete, class: 'btn btn-danger' %>
      </h1>
    </div>
  </div>
</div>
